import React from 'react';
import { Col, Row, Button, Card } from 'antd';

const OrderShippingOverviewCard = (props) => {
    return <Card bordered={false} title="Order Shipping Overview"></Card>;
};

export default OrderShippingOverviewCard;
