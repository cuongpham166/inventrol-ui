import React from 'react';
import { Col, Row, Button, Card } from 'antd';
const UserSummaryCard = (props) => {
    return <Card bordered={false} title="No. of users"></Card>;
};

export default UserSummaryCard;
